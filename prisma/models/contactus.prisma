model ContactMessage {
  id         String    @id @default(uuid()) @db.Uuid
  name       String
  email      String
  subject    String?
  message    String
  adminReply String?
  repliedAt  DateTime?
  createdAt  DateTime  @default(now())
}
